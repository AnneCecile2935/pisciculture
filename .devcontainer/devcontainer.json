{
  "name": "Django + PostgreSQL",
  "dockerFile": "Dockerfile",
  "mounts": [
    "source=${localWorkspaceFolder},target=/workspaces/pisciculture-1,type=bind,consistency=cached"
  ],
  "workspaceFolder": "/workspaces/pisciculture-1",
  "runArgs": ["--privileged"],
  "postCreateCommand": "sudo service docker start"
}
